/**
 * Действующая реализация аутентификации.
 *
 * Экраны импортируют её отсюда, а не из конкретного модуля: подмена реализации
 * (браузерная → серверная, а позже — внешний провайдер) не должна означать
 * правку каждой формы. Порт описан в `ports.ts`.
 */
export { httpAuthService as authService } from "./http-auth";
