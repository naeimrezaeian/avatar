/**
 * Имя куки сессии.
 *
 * Вынесено в отдельный модуль, потому что его читают и сервер, и proxy. Тянуть
 * ради одной строки серверный модуль с SQLite в proxy нельзя: он выполняется
 * перед каждым запросом, и открытие базы там ни к чему.
 */
export const SESSION_COOKIE = "avatar_session";
